$group

    set functors.comment
        function
            param parent
            param resultObj
            var commentObj
                {
                    @ author null
                    [ contents
            foreach child in parent.items
                if !fillContents(child, commentObj, resultObj)
            return commentObj
